// Kept only as a compatibility shim. The app is assembled in Hub.jsx, which is
// what main.jsx renders; the old single-file build that used to live here has
// been split into src/components, src/games, src/lib and src/profile.
export { default } from "./Hub.jsx";
